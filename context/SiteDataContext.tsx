// SiteDataContext — preloads shared data fetched once at app mount
// Provides: heroSlides, newsTicker, achievements, placementPartners
// (Data used by multiple components — fetched once, not per-component)
// Wrap <App> with <SiteDataProvider> in index.tsx
// TODO: implement
